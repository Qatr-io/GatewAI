apiVersion: v1
kind: Service
metadata:
  name: {{ include "gatewai-gateway.fullname" . }}
  labels:
    {{- include "gatewai-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gatewai-gateway.selectorLabels" . | nindent 4 }}
