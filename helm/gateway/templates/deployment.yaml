apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gatewai-gateway.fullname" . }}
  labels:
    {{- include "gatewai-gateway.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "gatewai-gateway.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gatewai-gateway.selectorLabels" . | nindent 8 }}
      annotations:
        # checksum/config is only set when configReloader is disabled.
        # When configReloader is enabled, it hot-reloads the config via POST /-/reload
        # without restarting pods — adding the checksum would trigger a rolling restart
        # on every ConfigMap change, defeating the purpose of the reloader.
        {{- if and (not .Values.config.existingConfigMap) (not .Values.configReloader.enabled) }}
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- end }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: gateway
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: CONFIG_PATH
              value: /etc/gatewai/config.yaml
            # Les credentials S3 sont injectés depuis le Secret et expansés
            # par os.Expand au démarrage (syntaxe ${VAR} dans config.yaml).
            - name: S3_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "gatewai-gateway.s3SecretName" . }}
                  key: S3_ACCESS_KEY
            - name: S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "gatewai-gateway.s3SecretName" . }}
                  key: S3_SECRET_KEY
            {{- if or .Values.encryption.key .Values.encryption.existingSecret }}
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "gatewai-gateway.encryptionSecretName" . }}
                  key: ENCRYPTION_KEY
            {{- end }}
            {{- if .Values.s3.existingCABundle }}
            - name: S3_CA_BUNDLE
              value: /etc/ssl/s3-ca/{{ .Values.s3.existingCABundleKey }}
            {{- end }}
            {{- with .Values.extraEnvVars }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: config
              mountPath: /etc/gatewai
              readOnly: true
            {{- if .Values.s3.existingCABundle }}
            - name: s3-ca
              mountPath: /etc/ssl/s3-ca
              readOnly: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        {{- if .Values.configReloader.enabled }}
        - name: config-reloader
          image: "{{ .Values.configReloader.image }}"
          args:
            - --volume-dir=/etc/gatewai
            - --webhook-url=http://localhost:8080/-/reload
            - --web.listen-address=:{{ .Values.configReloader.listenPort }}
          volumeMounts:
            - name: config
              mountPath: /etc/gatewai
              readOnly: true
        {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "gatewai-gateway.configMapName" . }}
        {{- if .Values.s3.existingCABundle }}
        - name: s3-ca
          configMap:
            name: {{ .Values.s3.existingCABundle }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
